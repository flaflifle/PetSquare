<div class="main">
  <h1>this is new place</h1>

<div class="row">
  <div class="large-6 columns">
    <fieldset>
      <legend>Parametri nuovo luogo</legend>
  <%= form_for(@place) do |f|%>
      <%= f.text_field :name, placeholder: 'nome posto' %>
      <%= f.text_field :category, placeholder: 'categoria' %>

      <%= f.text_field :street, placeholder: 'strada' %>
      <%= f.text_field :city, placeholder: 'city' %>

      <%= f.text_field :country, placeholder: 'country' %>
      <%= f.text_field :description, placeholder: 'descrizione' %>
          </fieldset>

      <%= f.submit 'Create new place', class: 'round primary small button' %>
  <% end %>
  </div>
</div>
</div>