<% provide :title, 'Home' %>
<div class="main">
<h1>This is HomePage</h1>
  <canvas id="esempio" width="100" height="100" style="border:1px solid black"></canvas>
  <script type="text/javascript">
      var canvas = document.getElementById('esempio');
      var c = canvas.getContext('2d');
      var img = new Image();
      img.addEventListener('load', function () {
          c.drawImage(this, 0, 0, 100, 100); //  ultimi due sono le dimensioni (larghezza altezza) dell immagine (scalamento)
      }, false);
      img.src = 'https://dl.dropboxusercontent.com/u/63254876/emanuele5.jpg';
  </script>

<% if signed_in? %>
      <div class="row">
        <div class="large-6 columns"></div>
    <%= link_to 'Cerca animale', searchPet_path, class: 'small round primary button' %>
        <div class="large-6 columns"></div>
    <%= link_to 'Cerca Posto', searchPlace_path, class: 'small round primary button' %>
    <fieldset>
      <legend>Ultime attività</legend>
      <%= render 'shared/feed' %>
                  </fieldset>
      <div class="divider"></div>
          <div class="large-4 columns"></div>
    <%= link_to 'Visualizza Animali che mi piacciono', following_user_path(@user) %>
      </div>
<% else  %>
    <p>New user? <%= link_to "Sign up now!", signup_path %></p>
<% end %>
</div>